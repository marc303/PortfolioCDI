@using BiblioPortCartier.ViewModels;
@model BiblioViewModel;

@{
    ViewData["Title"] = "Vos emprunts";
    Layout = "~/Views/Shared/_LibraryLayout.cshtml";
}

<h2 class="text-center mt-5">Vos emprunts - @Model.Member.FullName</h2>
@foreach (Loan obj in Model.LoansMember)
{
    <div class="card text-center mt-5">
        <div class="card-header">
            <h5>@obj.Document.Title</h5>
        </div>
        <div>
            <p>Créateur : @obj.Document.MakerName</p>
            <p>Année de publication : @obj.Document.PublishYear</p>
            <p>Categorie : @obj.Document.Category</p>
            <p>Type : @obj.Document.Rating</p>
            <p>Genre : @obj.Document.Genre</p>
        </div>
        <div class="card-footer text-body-secondary">
            <div class="row">
                <div class="col-6">
                    Date d'emprunt : @obj.BorrowedDate.ToLongDateString()
                </div>
                <div class="col-6">
                    Date de retour prévue : @obj.ScheduledReturnDate.ToLongDateString()
                </div>
            </div>
        </div>
    </div>
}